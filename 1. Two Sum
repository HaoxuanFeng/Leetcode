class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        listindex = len(nums)-1
        for x in (0,listindex):
            for y in (1,listindex):
                if nums[x] + nums[y] == target:
                    return x,y
